# Reviewer's Commands

- `architecture_review` — assesses a contract (in status DRAFT) against
  `AGENTS.md` and `memory/DECISIONS.md`, BEFORE implementation. Returns
  verdict `ACCEPTED`, `CHANGES_REQUESTED`, or `REJECTED`.
